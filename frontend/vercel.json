{
  "version": 2,
  "installCommand": "npm install && cd backend-src && npm install && npx prisma generate",
  "buildCommand": "rm -rf dist && npm run build && node ./node_modules/esbuild/bin/esbuild backend-src/index.ts --bundle --platform=node --format=cjs --outfile=api/dist/server.cjs --log-level=warning",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
